package com.nnk.appkira.presentation.designsystem.color

import androidx.compose.ui.graphics.Color

object AppColors {
    // Accent Color
    val Accent = Color(0xFFBD012A)

    // Dark Theme Palette
    val Dark = Color(0xFF20232B)
    val DarkX = Color(0xFF16181D)
    val DarkBackground = Color(0xFF1A1A1A)
    val DarkLight = Color(0xFF535A6F)

    // Light Theme Palette
    val Light = Color(0xFFEDEFF2)
    val LightX = Color(0xFFC8C8C8)

    // Text Colors (Dark Theme)
    val DarkTextMain = Color(0xFF283440)
    val DarkTextVar = Color(0xFF8491A2)
    val DarkTextLight = Color(0xFFAFAFAF)

    // Basic Colors
    val Green = Color(0xFF00C979)
    val Blue = Color(0xFF0A9DFF)
    val Black = Color(0xFF000000)
    val White = Color(0xFFFFFFFF)
    val Red = Color(0xFFff3447)

    // Transparent
    val Transparent = Color(0x00000000)
}
